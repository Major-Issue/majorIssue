package majorissue.com.gravity.outro;

import android.graphics.Typeface;

import majorissue.com.framework.Game;
import majorissue.com.gravity.intro.IntroText;

public class OutroText extends IntroText {

    public OutroText(Game game, String text, float relPosX, float relPosY, float textDuration, Typeface tf, int fs, int id) {
        super(game, text, relPosX, relPosY, textDuration, tf, fs, id);
    }
}
